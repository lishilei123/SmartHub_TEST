services:
  backend:
    build: ./backend
    environment:
      DB_PATH: /data/minitask.db
    volumes:
      - minitask_data:/data
    ports:
      - "8000:8000"
  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "8080:80"
volumes:
  minitask_data:
